[target.thumbv7em-none-eabihf]
# Cargo invokes the runner with the freshly-built ELF as $1. The script
# converts it to a .uf2 and copies it onto /Volumes/NICENANO if mounted.
runner = "./scripts/flash-uf2.sh"
rustflags = [
    "-C", "link-arg=-Tlink.x",
]

[build]
target = "thumbv7em-none-eabihf"
